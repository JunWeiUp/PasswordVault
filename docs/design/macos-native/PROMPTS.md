# Selected Mac screen prompts

Created with the built-in Image Gen tool. Every call received [the selected notes workspace](../macos-concepts/native-notes.png) as its visual reference. These are companion screens for one selected direction.

## Password detail

```text
Create ONE high-fidelity password-detail screen for the selected PasswordVault native macOS design. Treat the attached image as the STRICT visual target, not loose inspiration. User selected this exact light native-notes direction. Preserve its window silhouette, traffic lights, three-column shell, sidebar width, vertical separators, typography, blue accent, button corners, and comfortable white space. Output one full front-on macOS app window; target canvas 1440 x 1024 (same natural proportions as the attached reference). No wallpaper, laptop, browser UI, collage, extra screens, or option number.

Change the active destination from notes to 密码. Keep the left navigation visually consistent with the reference: exact small PasswordVault P/vault logo and brand, note shortcuts 全部笔记 / 家庭 / 个人 / 工作, then 密码 selected with pale blue highlight, 验证码 and 钱包; 工具 and 设置 near the bottom. Reuse the icons and their visual weight. Do not redesign branding or add new categories.

The middle column is a clean flat account list. Title 密码, compact blue + 新建账号 button, 搜索账号 search field with ⌘K hint. Five fictional rows, no stacked cards: 家庭邮箱 selected, 云盘, 购物账号, 工作协作, 示例服务. Each has a small understated service glyph, account display name and username in subdued text. All usernames use reserved example.com addresses, chosen username family@example.com. The selected row uses the reference light-blue tint.

The right detail pane is the main focus. Toolbar breadcrumb 家庭 / 家庭邮箱; favorite outline star, small 更多 ellipsis, blue 编辑 action, and far-right 锁定 control. Under the toolbar a small quiet checkmark 已加密保存. Title 家庭邮箱 in exactly the reference heading style, subtitle 家庭日常收件与通知.
Show spacious aligned native-style field rows separated by fine lines:
用户名
family@example.com       [copy icon with 复制 label]
密码
••••••••••••            [eye icon] [copy icon with 复制 label]
网站
https://mail.example.com       [external link icon]
Each field's label is muted small text, values are readable around 16px, no large filled cards or oversized controls. Password MUST stay masked, never generate an actual secret.
Then a collapsed row 其他账号 2 with disclosure chevron and a separate collapsed row 密码历史 with disclosure chevron. Do not show new unsupported functionality.
Lower down section 备注 with a short paragraph 用于家庭服务通知，不用于工作。 Small inline category 家庭 and tag 个人 may be placed quietly near note text if needed. Tiny footer 示例内容. Leave breathing room below; do not fill it with metrics, health scores, recommendations or promotion.

All UI in precise Simplified Chinese except PasswordVault, reserved example URLs and keyboard labels. Make text crisp and readable, with 14–16px body conventions. No huge badges, gradients, card grids, or security guarantee claims. Maintain all controls within bounds. This is a sibling screen of the chosen design, so strong consistency matters more than novelty. Image is a design mock, not an implemented-app capture. Time anchor from handoff 2026-09-23 10:49:09; no dates need to be displayed.
```

## Touch ID unlock

```text
Create ONE high-fidelity Touch ID unlock / locked-state screen for the chosen PasswordVault native macOS app. The attached screenshot is the STRICT selected visual system. Preserve its Mac window shape, upper traffic lights and titlebar height, precise PingFang/SF Pro-like typography, cool very light gray and white surfaces, blue #315CE7, small brand mark, button corners, and restrained outline icon style. Target canvas 1440 x 1024 with the reference's natural proportions; a single app window fills the image. No device mockup, wallpaper, browser chrome, collage, presentation captions or option numbers.

This is the LOCKED state: all vault content must be hidden. DO NOT display the reference note title, categories, counts, email, list rows, breadcrumbs, account names, or even blurred readable private content. Completely replace the underlying three-column document with a plain neutral cool-white canvas, no ghosted content. Top titlebar can show the public brand PasswordVault only. Keep traffic lights upper left. This is the app's own unlock page, not a fabricated macOS system authorization dialog.

Centered within the large window, build a quiet, beautifully balanced unlock composition with generous white space and no enclosing elevated card:
- A faithful small version of the selected P/vault app mark, about 58px, not oversized.
- Heading PasswordVault 已锁定, about 28px, ink #202634.
- Subtext 解锁后查看你的笔记和密码, muted gray, around 16px.
- A subtle native-style Touch ID fingerprint symbol, approximately 52px, blue/gray, not cartoon art.
- Primary blue button 使用 Touch ID 解锁, around 300px wide and 42px high.
- Secondary text button 使用主密码, visibly available under the primary button.
- A short muted helper text Touch ID 不可用时，可使用主密码解锁。
No loading spinner or fake success state; this represents the resting state before user action. Do not show a password field simultaneously because the secondary action opens the password form. Do not claim online password reset, recovery email, cloud login, audit, or guaranteed security.

In the bottom-right of the app window, a small native language selector 简体中文 with a subtle globe icon. Optional footer text 数据保存在本机, plain and muted, without a shield/badge. The screen must look intentional at desktop scale, with elements located just above vertical center, not a stretched mobile login UI.
All visible text accurate Simplified Chinese except PasswordVault and Touch ID. Body remains crisp and readable. No gradients, card-on-card layout, dramatic shadows, promotional visuals, huge locks or security metrics. This is a sibling concept for the user's selected native light design; consistency is paramount. Clock anchor 2026-09-23 10:49:09 from handoff; do not add timestamps. One image only.
```

## Security settings

```text
Create ONE high-fidelity macOS settings screen for PasswordVault, extending the STRICT selected light native-notes reference image. Preserve the selected window frame, traffic lights, sidebar, 3-column structure, cool pale gray/white surfaces, fine vertical separators, small exact brand mark, SF Pro/PingFang-like Chinese type, blue #315CE7, and modest rounded controls. Target 1440 x 1024 in the same natural proportions as the reference. One full app window front-on; no wallpaper, device mockup, browser chrome, collage, option number, or surrounding presentation text.

Make a settings page, NOT a dashboard or feature inventory. Exactly the same app shell as the chosen reference:
Left column: faithful small PasswordVault logo + title, normal primary note/account destinations matching reference (全部笔记, 家庭, 个人, 工作, 密码, 验证码, 钱包), collapsed 工具 near bottom, 设置 selected with pale blue highlight at bottom. Keep any small counts secondary; no private note names or content shown here.
Middle column: title 设置, small search input 搜索设置, then five clean native rows with subtle outline icons 通用, 安全 (selected in reference's pale blue), 外观, 浏览器, 备份与同步. Flat simple rows and comfortable spacing, no button grid or cards. Most of this column can remain quiet blank space.
Right pane: subtle toolbar breadcrumb 设置 / 安全, far right lock control 锁定. Heading 安全 in the same weight and scale as the chosen note title, secondary text 管理解锁方式与自动锁定。
Below it a native Mac form with a comfortable content width and horizontal fine separators. Use 14–16px labels, quiet gray helper lines and native blue switches; no giant status badges or security scores.

Section 解锁方式:
row 主密码 with outlined secondary button 修改主密码…
row 使用 Touch ID with macOS toggle ON at far right, helper 仅在这台 Mac 上启用快捷解锁。
small helper below the row Touch ID 不可用时，仍可使用主密码。
Section 自动锁定:
row 闲置后锁定 with native dropdown 10 分钟.
row 系统锁屏或睡眠时 with fixed right-aligned value 立即锁定. This is read-only, NOT a disableable switch, so mandatory lock protection cannot accidentally be switched off.
Section 锁定后的显示:
row 笔记和账号内容 with fixed value 全部隐藏.
helper 解锁后继续查看和编辑。
At bottom of the form put a modest outlined 锁定资料库 button. No reset/delete vault control, cloud signup, unrequested settings, health statistics, clipboard timer, or additional authentication options. Leave generous quiet space below the form rather than filling it with decoration.

This is a concept for settings that will later be implemented; do not add claims of audit or absolute protection. All visible text should be accurate Simplified Chinese except PasswordVault and Touch ID. Keep form labels and controls aligned and contained within the viewport, high contrast and crisp. No dark theme, gradients, cards within cards, or giant icons. One independent sibling screen in the already-selected visual system. Time anchor 2026-09-23 10:49:09; no dates required.
```
